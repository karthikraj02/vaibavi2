/**
 * @deprecated Duffel integration has moved to the main backend API.
 * Use POST /api/duffel/search on the server started via `cd backend && npm start`.
 *
 * This standalone file is kept for reference only.
 */
console.log('Duffel flights are now integrated at /api/duffel/* on the main backend (port 5000).');
console.log('Start the backend with: cd backend && npm start');
